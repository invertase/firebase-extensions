# Error Handling

Errors are returned as plain text responses with a 400 status code. The response body will contain the error message.

## Sample output

Aan example with unknown operation would produce the following error response.

```
Options for the 'test' operation (at position 0) are invalid: Operation of type 'test' does not exist or is unsupported.
```
